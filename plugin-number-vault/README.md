# 数字密匣

单人社区插件案例。系统从 1–20 中生成一个秘密数字，玩家有 6 次机会，根据“更大/更小”提示完成破解。

这个案例演示：

- 单人插件根据 manifest 的 `players.max: 1` 自动创建并开始房间。
- 秘密答案只保存在后端状态，结束前不会通过 `view()` 发给前端。
- 插件调用公共动作 SDK，并沿用返回、设置、战绩、排行榜和重开机制。
- 插件源码、界面和测试全部留在当前目录。

前端动作：`guess`，payload 为 `{ "value": 1–20 }`。
